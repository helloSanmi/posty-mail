// Registry of redesigned pages available in the sandbox.
//
// One entry per page as it is designed. Order here is the order of the Page
// switcher, and should follow the agreed sequence: the shell and theme
// first, then Home, Campaigns, Audience, Reports, Email, Builder, and the
// small pages last.
//
// Empty until the first design lands — the sandbox falls back to the token
// sheet so the palette can still be reviewed.
export const DESIGN_PAGES = [];
