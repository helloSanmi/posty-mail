// Vitest setup file. Runs once before any UI test. Pulls in jest-dom's
// custom matchers (e.g. toBeInTheDocument, toHaveTextContent) so tests can
// assert against the rendered DOM in human-readable form.
import '@testing-library/jest-dom/vitest';
